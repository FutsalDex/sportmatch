
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents basic user information, optimized for search and filtering queries.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "name": {
          "type": "string"
        },
        "email": {
          "type": "string",
          "description": "The user's primary email address.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The user's role (e.g., 'Player', 'Coach', 'Club')."
        },
        "discipline": {
          "type": "string",
          "description": "The sport discipline (e.g., 'Football', 'Futsal')."
        },
        "status": {
          "type": "string",
          "description": "Current availability status."
        },
        "score": {
          "type": "number",
          "description": "Relevance score for rankings."
        },
        "province": {
          "type": "string",
          "description": "The geographic province from the official resources list."
        },
        "level": {
          "type": "string",
          "description": "Professional or skill level."
        },
        "verificationStatus": {
          "type": "string",
          "description": "Certification verification status."
        },
        "age": {
          "type": "number"
        },
        "position": {
          "type": "string"
        },
        "profileImageUrl": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "email",
        "role",
        "discipline",
        "status",
        "score",
        "verificationStatus"
      ]
    },
    "Province": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Province",
      "type": "object",
      "description": "Official province entry for location normalization.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" }
      },
      "required": ["id", "name"]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Detailed biographical and media content.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "bio": { "type": "string" },
        "teamHistory": { "type": "array", "items": { "type": "string" } },
        "videoUrls": { "type": "array", "items": { "type": "string" } },
        "height": { "type": "number" },
        "weight": { "type": "number" },
        "strongFoot": { "type": "string" },
        "bookImageUrls": { "type": "array", "items": { "type": "string" } },
        "summary": { "type": "string" },
        "analysis": { "type": "string" }
      },
      "required": ["id", "userId", "bio"]
    },
    "PrivateProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PrivateProfile",
      "type": "object",
      "description": "Sensitive contact information.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "whatsappNumber": { "type": "string" },
        "emailContact": { "type": "string", "format": "email" },
        "address": { "type": "string" }
      },
      "required": ["id", "userId", "phoneNumber", "emailContact"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": { "$ref": "#/backend/entities/User" },
          "description": "Publicly searchable user identities."
        }
      },
      {
        "path": "/recursos/espana/provincias/{provinceId}",
        "definition": {
          "entityName": "Province",
          "schema": { "$ref": "#/backend/entities/Province" },
          "description": "Normalized list of Spanish provinces for profile consistency."
        }
      },
      {
        "path": "/userProfiles/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": { "$ref": "#/backend/entities/UserProfile" },
          "description": "Rich public profile content."
        }
      }
    ]
  }
}
